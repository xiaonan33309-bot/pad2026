# pad2026

这是一个完整的 Android（Kotlin + XML）项目骨架，包含：

- `app` 模块
- 基础 `MainActivity`
- 布局与主题资源
- 单元测试与仪器测试示例

## 本地运行

1. 安装 Android Studio（建议最新版）与 Android SDK（包含 API 35）。
2. 在项目根目录执行：

```bash
./gradlew assembleDebug
```

3. 在 Android Studio 中打开该目录并运行 `app`。
